RegisterNUICallback('set_job', function(data, cb)
    data.staff = GetPlayerName(PlayerId())

end)

RegisterNUICallback('edit_money', function(data, cb)
    data.staff = GetPlayerName(PlayerId())

end)

RegisterNUICallback('edit_gold', function(data, cb)
    data.staff = GetPlayerName(PlayerId())

end)

RegisterNUICallback('edit_staff_perms', function(data, cb)
    data.staff = GetPlayerName(PlayerId())

end)